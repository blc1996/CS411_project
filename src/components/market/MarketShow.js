import React from 'react';
import { connect } from 'react-redux';

class MarketShow extends React.Component {
    render () {
        return (
            <div>
                MarketShow
            </div>
        );
    } 
};

export default connect(null, {})(MarketShow);
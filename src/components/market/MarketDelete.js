import React from 'react';
import { connect } from 'react-redux';

class MarketDelete extends React.Component {
    render () {
        return (
            <div>
                MarketDelete
            </div>
        );
    } 
};

export default connect(null, {})(MarketDelete);